# Development (SQLite)
driver: sqlite3
dsn: pt.db

# Production (PostgreSQL)
# driver: postgres
# dsn: "host=localhost user=pt password=pt_password dbname=pt_db port=5432 sslmode=disable"

listen_addr: ":8080"
jwt_secret: "change-me-to-a-random-secret"
upload_dir: "./uploads/torrents"
